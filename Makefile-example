include .env
export

#####################################################################

DOCKER_COMPOSE = /usr/local/bin/docker-compose -p ${PROJECT_NAME}
DOCKER_COMPOSE_CRON = ${DOCKER_COMPOSE} -f ${USERSIDE_BASE_DIR}/docker-compose.yml --env-file ${USERSIDE_BASE_DIR}/.env

define INSTALLER_JSON
{
  "pgsql_dsn": "pgsql:host=postgres;dbname=${POSTGRES_DB};port=5432;username=${POSTGRES_USER};password=${POSTGRES_PASSWORD}",
  "redis_url": "redis://${REDIS_PASSWORD}@redis",
  "rabbit_url": "amqp://${RABBITMQ_DEFAULT_USER}:${RABBITMQ_DEFAULT_PASS}@rabbitmq:5672/%2f",
  "stability": "stable"
}
endef
export INSTALLER_JSON

define CRON_TASKS
* * * * *    root    ${DOCKER_COMPOSE_CRON} run --rm php php userside cron > /dev/null
0 3 * * *    root    ${DOCKER_COMPOSE_CRON} run --rm php /app/backup.sh > /dev/null
0 4 * * *    root    ${DOCKER_COMPOSE_CRON} exec postgres /app/backup.sh > /dev/null
#*/10 * * * *   root    ${DOCKER_COMPOSE_CRON} run --rm usm_billing > /dev/null
endef
export CRON_TASKS

#####################################################################

up: bundle-up
down: bundle-stop
stop: bundle-stop
restart: down up
install: create-init-dirs-and-files bundle-up us-install cron-setup cron-run rabbitmq-cleanup need-restart
userside-install: us-install
userside-repair: us-repair
update: backup us-cache-flush bundle-stop bundle-pull cron-remove install postgres-postgis-update
uninstall: bundle-destroy cron-remove
log: bundle-logs
flush: us-cache-flush

#####################################################################

bundle-up:
	${DOCKER_COMPOSE} up -d --scale core-worker=${CORE_WORKER_NUM} --scale poller=${POLLER_NUM}

bundle-stop:
	${DOCKER_COMPOSE} stop

bundle-destroy: confirmation
	${DOCKER_COMPOSE} down -v --remove-orphans

bundle-logs:
	${DOCKER_COMPOSE} logs -f

bundle-pull:
	${DOCKER_COMPOSE} pull

####

create-init-dirs-and-files:
	mkdir -p ${USERSIDE_BASE_DIR}/data/db ${USERSIDE_BASE_DIR}/data/backup ${USERSIDE_BASE_DIR}/data/userside
	mkdir -p ${USERSIDE_BASE_DIR}/data/userside/var/log/usm_billing
	@test -f ${USERSIDE_BASE_DIR}/data/userside/.env || echo "" >> ${USERSIDE_BASE_DIR}/data/userside/.env
	@test -f ${USERSIDE_BASE_DIR}/data/userside/installer.json || \
		echo "$$INSTALLER_JSON" > ${USERSIDE_BASE_DIR}/data/userside/installer.json

us-install:
	${DOCKER_COMPOSE} run --rm php /app/userside_install.sh install

us-repair:
	${DOCKER_COMPOSE} run --rm php /app/userside_install.sh repair

us-cache-flush:
	${DOCKER_COMPOSE} run --rm php php userside cache/flush-all

us-change-stability:
	${DOCKER_COMPOSE} run --rm php /app/userside_install.sh config --stability=${STABILITY_LEVEL}

####

postgres-psql:
	${DOCKER_COMPOSE} exec postgres psql -U ${POSTGRES_USER} -d ${POSTGRES_DB}

postgres-postgis-update:
	${DOCKER_COMPOSE} exec postgres psql -U ${POSTGRES_USER} -d ${POSTGRES_DB} -c "ALTER EXTENSION postgis UPDATE"

####

rabbitmqctl:
	${DOCKER_COMPOSE} exec rabbitmq rabbitmqctl

rabbitmq-cleanup: rabbitmq-stop rabbitmq-reset rabbitmq-start

rabbitmq-stop:
	${DOCKER_COMPOSE} exec rabbitmq rabbitmqctl stop_app

rabbitmq-reset:
	${DOCKER_COMPOSE} exec rabbitmq rabbitmqctl reset

rabbitmq-start:
	${DOCKER_COMPOSE} exec rabbitmq rabbitmqctl start_app

####

backup:
	${DOCKER_COMPOSE} exec postgres /app/backup.sh
	${DOCKER_COMPOSE} run --rm php /app/backup.sh

dbrestore:
	${DOCKER_COMPOSE} exec postgres /app/restore.sh ${DUMP}

####

cron-setup:
	@echo "Add crontab tasks:"
	@echo "$$CRON_TASKS" | sudo tee /etc/cron.d/${PROJECT_NAME}

cron-run:
	${DOCKER_COMPOSE} run --rm php php userside cron

cron-remove:
	sudo rm -f /etc/cron.d/${PROJECT_NAME}

####

confirmation:
	@( read -p "This action will DELETE all data of ${PROJECT_NAME}!!! Are you sure? [y/N]: " sure && case "$$sure" in [yY]) true;; *) false;; esac )

need-restart:
	@echo ""
	@echo "\e[1m\e[31mThe USERSIDE bundle need to be restart!\e[0m\n"
	@echo ""
	@echo "Please run:"
	@echo ""
	@echo "    \033[1msudo make restart\033[0m"
	@echo ""
	@echo ""